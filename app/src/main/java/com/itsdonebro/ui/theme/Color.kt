package com.itsdonebro.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand palette ────────────────────────────────────────────────────────────
val BrandRed      = Color(0xFFFF3B30)   // Primary — the 🔴 color
val BrandOrange   = Color(0xFFFF9500)   // Warning accent
val BrandGreen    = Color(0xFF30D158)   // Success / under-limit
val BrandBlue     = Color(0xFF0A84FF)   // Info

// ─── Backgrounds ──────────────────────────────────────────────────────────────
val Background    = Color(0xFF0A0A0C)
val Surface       = Color(0xFF141417)
val SurfaceVariant= Color(0xFF1E1E24)
val SurfaceCard   = Color(0xFF1C1C1F)

// ─── Text ─────────────────────────────────────────────────────────────────────
val OnBackground  = Color(0xFFFFFFFF)
val OnSurface     = Color(0xFFEAEAEA)
val OnSurfaceDim  = Color(0xFF8E8E9A)
val OnSurfaceFaint= Color(0xFF48484F)

// ─── Dividers / borders ───────────────────────────────────────────────────────
val Divider       = Color(0xFF2A2A30)

// ─── Gradients (used as brush lists) ─────────────────────────────────────────
val GradientRed   = listOf(Color(0xFFFF3B30), Color(0xFFFF6B35))
val GradientDark  = listOf(Color(0xFF141417), Color(0xFF0A0A0C))
